import StickyCallBar from '../StickyCallBar'

export default function StickyCallBarExample() {
  return (
    <div className="h-screen bg-background">
      <StickyCallBar />
      <p className="p-8">Scroll down to see the sticky call bar appear...</p>
    </div>
  )
}
